<section class="vh-100 gradient-custom">
  <div class="container py-3 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-6">
        <div class="card bg-transparent text-white" style="border-radius: 1rem;">
          <div class="card-body p-5 text-center">
            <div class="mb-md-5 mt-md-4 pb-5">
              <h2 class="fw-bold mb-2 text-uppercase">Forgot your password?</h2>
              <p class="text-white-50 mb-5">Please enter your Email or Username</p>
              <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
                <%= render "users/shared/error_messages", resource: resource %>
                <div class="form-outline mb-4">
                  <%= f.label :username_or_password, class:"form-label"  %><br />
                  <%= f.text_field :login, autofocus: true, class: "form-control form-control-lg bg-transparent", style: 'color: white;' %>
                </div>
                <div class="d-flex justify-content-center">
                  <%= f.submit "Send me reset password instructions", class:"btn btn-outline-light btn-lg px-3", data: { turbo: false }  %>
                </div>
              <% end %>
              <p class="text-center text-muted mt-5 mb-0">
                <%= render "users/shared/links" %>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
